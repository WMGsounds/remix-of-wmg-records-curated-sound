// Music page hero image.
// Same mechanism as src/assets/videosHero.ts — paste the optimised
// base64 data URL (or an imported asset URL) here and the hero layer
// renders automatically. While empty, the hero shows the gradient
// treatment only, exactly like the other WMG heroes without an image.
export const musicHeroDataUrl = "";
